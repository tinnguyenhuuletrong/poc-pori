import * as path from 'path';
export const environment = {
  dbPath: path.join(
    __dirname,
    '../../../../archived/repo/allEvents.stag.realm'
  ),
  createdBlock: 25777543,
  production: false,
};
